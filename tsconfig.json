{
  "compilerOptions": {
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "@clumsy_chinchilla/*": ["lib/browser/*"] // This mapping is relative to "baseUrl"
    }
  }
}
