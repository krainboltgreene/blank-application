#!/bin/bash

project_id=$(gcloud config get-value core/project)
bucket="${project_id}-releases"

echo "Copying binary to release location..."
gsutil cp tmp/clumsy_chinchilla.run gs://${bucket}/latest
