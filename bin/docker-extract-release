#!/bin/sh

container_id=$(docker create application-production:latest)

docker cp ${container_id}:/application/_build/prod/rel/clumsy_chinchilla/bin/clumsy_chinchilla.run tmp/clumsy_chinchilla.run

docker rm ${container_id}
